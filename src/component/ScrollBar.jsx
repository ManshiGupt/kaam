import React, { useState } from 'react'

const ScrollBar = () => {
    const [data,setData]= useState([]);
    const dat=[
        {
            name:"manshi",
    }
    ]
  return (
    <div>ScrollBar</div>
  )
}

export default ScrollBar